alias ll='if command -v exa &>/dev/null; then exa -lF --group-directories-first; else ls -l; fi'
alias lla='if command -v exa &>/dev/null; then exa -lF --group-directories-first --git --all; else ls -la; fi'  
